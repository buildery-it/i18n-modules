{
  "scripts": {
    "reinstall": "rm -rf node_modules package-lock.json && npm install",
    "export": "NEXT_TELEMETRY_DEBUG=1 NEXT_TELEMETRY_DISABLED=1 NODE_ENV=production next export",
    "prod": "NEXT_TELEMETRY_DEBUG=1 NEXT_TELEMETRY_DISABLED=1 NODE_ENV=production next start",
    "build": "NEXT_TELEMETRY_DEBUG=1 NEXT_TELEMETRY_DISABLED=1 NODE_ENV=production next build",
    "start": "NEXT_TELEMETRY_DEBUG=1 NEXT_TELEMETRY_DISABLED=1 next dev"
  },
  "devDependencies": {
    "next": "^13.14.12",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
